#built application files
*.apk
*.ap_

# files for the dex VM
*.dex

# Local configuration file (sdk path, etc)
local.properties

# Android Studio
.idea/

# Markdown Navigator is an Intellij plugin for editing markdown files.
# It adds settings into the `.idea/` folder which we probably want to ignore.
.idea/markdown-navigator/profiles_settings.xml
.idea/markdown-navigator.xml
.idea/markdown-exported-files.xml

# New Relic upload produces a mapping.txt file it produces before
# uploading it to their servers. It leaves it around, presumably so that
# CI could upload it itself? Split the commands to produce the file and
# upload it later? Either way, just ignore it.
mapping.txt

.gradle
*/out
*build/
*/production
*/release
*.iml
*.iws
*.ipr
*~
*.swp
caches/
class_files.txt
seeds.txt
unused.txt
captures/

# Android project files#
########################
google-play-services_lib/
*/bin/
*/gen/
*/example/
*/sample/
AndroidUnusedResources1.6.2.jar
build/

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o


# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Jacoco #
##########
*jacoco.exec

# Kotlintest
.kotlintest/

# NPM dependencies #
# No need to include package dependencies as the `yarn.lock` file already stores dependency versions info.
#
node_modules/
